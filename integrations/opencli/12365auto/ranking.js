import { cli, Strategy } from '@jackwener/opencli/registry';
const BASE = 'https://www.12365auto.com';
const clean = (v) => String(v ?? '').replace(/\s+/g, ' ').trim();
const html = async (url) => { const r = await fetch(url, { headers: { 'user-agent': 'SourcePort/1.0 read-only' } }); if (!r.ok) throw new Error(`12365auto HTTP ${r.status}`); return await r.text(); };
const rows = (body, query) => [...body.matchAll(/href=["']([^"']*\/zlts\/[^"']*)["'][^>]*>([^<]{2,120})</gi)].map((m) => ({ complaintId: (m[1].match(/(\d+)/)?.[1] ?? ''), brand: '', series: clean(query), model: '', summary: clean(m[2]), submittedAt: '', status: 'unknown', url: new URL(m[1], BASE).href })).filter(x => x.complaintId);
cli({ site:'12365auto', name:'ranking', description:'ranking (read-only)', access:'read', domain:'12365auto', strategy:Strategy.PUBLIC, browser:false, args:[{name:'query', positional:true, required:true, help:'Brand or series keyword'},{name:'limit',type:'int',default:20,help:'Maximum rows (1-50)'}], columns:['complaintId','brand','series','model','summary','submittedAt','status','url'], func:async ({query,limit=20}) => { const body=await html(`${BASE}/zlts/`); const out=rows(body, query).filter(x => !query || `${x.summary} ${x.series}`.includes(query)).slice(0, Math.min(Number(limit)||20,50)); if(!out.length) throw new Error('12365auto returned no usable complaints'); return out; } });
